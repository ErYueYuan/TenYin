<!--toast.wxml-->

<!--mask 背景-->
<view class="toast_mask3" wx:if="{{_isShowMask}}"></view>
<!--以下为toast显示的内容-->
<view class="toast_content_box3" wx:if="{{_isShowToast}}">
  <view class="toast_content3" wx:if="{{_isShowToast}}">
    <view class='text3'>{{_toastText}}</view>
  </view>
</view>